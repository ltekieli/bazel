module(
    name = "lib_e",
    version = "0.0.0",
)

bazel_dep(name = "lib_a", version = "0.0.0")

# Extend lib_a's configurable target with a target that has output groups
extend_target(
    target = "@lib_a//:content",
    extra = "//:extra_with_groups",
)
