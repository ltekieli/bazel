module(
    name = "example_error_not_configurable",
    version = "0.0.0",
)

bazel_dep(name = "lib_b", version = "0.0.0")
local_path_override(
    module_name = "lib_b",
    path = "../lib_b",
)

bazel_dep(name = "lib_a", version = "0.0.0")
local_path_override(
    module_name = "lib_a",
    path = "../lib_a",
)

# ERROR: lib_b//:custom_content is a filegroup, not a configurable_target.
extend_target(
    target = "@lib_b//:custom_content",
    extra = "//:extra_content",
)
